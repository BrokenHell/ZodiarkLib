namespace ZodiarkLib.Data
{
    /// <summary>
    /// Base class for runtime data
    /// Memory data is a data that only stored on memory can't be saved after each sessions
    /// </summary>
    public interface IMemoryData : IData
    {
    }
}